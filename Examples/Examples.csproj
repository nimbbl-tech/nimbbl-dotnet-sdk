<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Use local SDK source (default within this repo — always matches the code being developed) -->
  <ItemGroup>
    <ProjectReference Include="..\Nimbbl.Sdk.Rest\Nimbbl.Sdk.Rest.csproj" />
  </ItemGroup>

  <!-- Use published SDK from NuGet (uncomment to build against the released package instead) -->
  <!--
  <ItemGroup>
    <PackageReference Include="Nimbbl.Sdk.Rest" Version="1.4.0" />
  </ItemGroup>
    -->

</Project>

